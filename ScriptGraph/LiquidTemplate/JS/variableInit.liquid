        // init all variables

    {% for variable in variableList %}
        this.variables["{{variable.variableName}}_{{variable.variableIndex}}"] = {{variable | getVariableValueExpression}}
    {% endfor %}
